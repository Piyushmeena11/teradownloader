{
  "name": "TeraDownloader",
  "description": "Premium Terabox Downloader Web Tool - Download files from Terabox without registration",
  "repository": "https://github.com/yourusername/teradownloader",
  "logo": "https://node-js-sample.herokuapp.com/node.png",
  "keywords": ["terabox", "downloader", "node", "express", "react", "vite"],
  "success_url": "/",
  "buildpacks": [
    {
      "url": "heroku/nodejs"
    }
  ],
  "env": {
    "NODE_ENV": {
      "description": "Node environment",
      "value": "production",
      "required": false
    },
    "ALLOWED_ORIGINS": {
      "description": "Comma-separated list of allowed CORS origins (will be set automatically after deployment)",
      "required": false
    }
  },
  "formation": {
    "web": {
      "quantity": 1,
      "size": "free"
    }
  }
}
