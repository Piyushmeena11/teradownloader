{
  "name": "teradownloader",
  "version": "1.0.0",
  "description": "Premium Terabox Downloader Web Tool",
  "type": "module",
  "scripts": {
    "dev": "concurrently \"npm run dev:server\" \"npm run dev:client\"",
    "dev:server": "cd server && npm run dev",
    "dev:client": "cd client && npm run dev",
    "build": "echo '🏗️  Building React app...' && cd client && npm run build && echo '✅ Build completed'",
    "start": "node start.js",
    "install:all": "echo '📦 Installing root dependencies...' && npm install && echo '📦 Installing server dependencies...' && cd server && npm install && echo '📦 Installing client dependencies...' && cd ../client && NPM_CONFIG_PRODUCTION=false npm install && echo '✅ All dependencies installed'",
    "verify-build": "node scripts/verify-build.js",
    "heroku-postbuild": "node scripts/heroku-build.js"
  },
  "keywords": ["terabox", "downloader", "file-downloader"],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "engines": {
    "node": "18.x",
    "npm": "9.x"
  }
}

